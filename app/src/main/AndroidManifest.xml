<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fgwoa.fgwmobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="activity.SplashActivity" android:screenOrientation="landscape"
            android:theme="@style/AppTheme.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="activity.MainActivity"></activity>

        <activity android:name="activity.GwqpActivity">
        </activity>

        <activity android:name="activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="landscape">
        </activity>

        <activity android:name="gwQuery.GwQueryActivity">
        </activity>
        <activity android:name="gwQuery.QueryListActivity">
        </activity>

        <activity android:name="activity.GwppActivity"
            android:label="@string/gongwenpibandan"
            android:screenOrientation="landscape">
        </activity>

    </application>

</manifest>
